### Exercise 3

Create a component to display Hero

```{TypeScript}
import {Component, Input} from '@angular/core';

@Component({
  selector: 'hero',
  template: '{{name}}'
})
export class HeroComponent {
  @Input() name: string;
}

==========

import {HeroComponent} from './hero.component';

@Component({
  selector: 'heroes',
  template: `
  <h1>Heroes</h1>
  <ul>
    <li *ngFor="let name of names">
      <hero [name]="name"></hero>
    </li>
  </ul>
  `,
  directives: [HeroComponent]
})

```
